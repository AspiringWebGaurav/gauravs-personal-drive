rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Personal use rules - authenticated users can read/write anywhere
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}